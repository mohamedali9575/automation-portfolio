# 02 REST API Tests — JDK HttpClient + TestNG

Tests https://jsonplaceholder.typicode.com (free fake API). No extra deps.

- `getUsersReturns200WithExpectedFields`
- `getSinglePostReturnsCorrectUserId`
- `createPostReturns201`

Run: `mvn test`
