# 01 Login UI Tests — Playwright + TestNG

Tests https://www.saucedemo.com/ (standard demo shop).

- `validLoginShowsInventory` — happy path
- `invalidLoginShowsError` — data-driven: locked user, wrong password, empty fields

Run: `mvn test`
